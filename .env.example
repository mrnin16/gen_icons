# Per-app env files live alongside each app:
#   apps/api/.env  — DATABASE_URL + AI provider keys
#   apps/web/.env  — NEXT_PUBLIC_API_URL
#
# See apps/api/.env.example and apps/web/.env.example for templates.
