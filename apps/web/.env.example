# URL of the API service the web UI talks to.
# - Local dev: point at the local API service (default port 3001)
# - Production: set to the API's public Railway URL
NEXT_PUBLIC_API_URL="http://localhost:3001"
